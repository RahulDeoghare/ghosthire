# secrets
.env

# database — rebuilt from snapshots via `python -m ghosthire.db init`
*.db
*.db-journal
# WAL sidecars, created by the journal_mode=WAL pragma
*.db-wal
*.db-shm
*.sqlite3

# python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/

# node / web
node_modules/
.vercel/

# os
.DS_Store

# planning notes — not part of the shipped project
plan.md
PROTOTYPES.md

# in-flight snapshot reservations — never evidence, never committed
data/snapshots/*.part

# snapshot reservations — an in-flight or abandoned run, never evidence
*.json.part
