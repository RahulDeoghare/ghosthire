# Pinned to what is installed and verified on this machine
# (Python 3.14.6 / macOS arm64). Keep these in step with `pip list`.

fastapi==0.141.1
uvicorn[standard]==0.52.3
rapidfuzz==3.14.5
python-dateutil==2.9.0.post0
python-dotenv==1.2.3
PyYAML==6.0.3          # collectors.yaml; also arrives via uvicorn[standard]

# dev
pytest==9.1.1
ruff==0.9.6
